[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.111", "uvicorn[standard]>=0.30", "pydantic>=2.7", "httpx>=0.27", "networkx>=3.3", "firebase-admin>=6.5", "mangum>=0.17", "python-dotenv>=1.0", "pytest>=8.2", "pytest-asyncio>=0.23", "pypdf>=5.0" ]
requires-python = "~=3.12.0"
